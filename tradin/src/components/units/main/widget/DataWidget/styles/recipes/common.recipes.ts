import { defineRecipe } from "@chakra-ui/react";

export const dataWidgetTitleRecipe = defineRecipe({
    base: {
        fontSize: "13px",
        fontWeight: "700",
        width: "100%",
        color: "textColor",
        marginBottom: "5px",
    }
})