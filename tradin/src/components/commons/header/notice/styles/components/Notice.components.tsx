"use client";

import { chakra, Flex, Button, Box, IconButton } from "@chakra-ui/react";
import { bellIconButtonRecipe } from "../recipes/notice.recipes";

export const BellIconButton = chakra(IconButton, bellIconButtonRecipe);
